<div style="background-image:url('{{asset('images/img-de-la-chapelle.jpg')}}'); background-position: center; background-repeat: no-repeat; background-size: cover; width=auto; height: 564px;" >
      <nav class="navbar navbar-expand-lg">
        <a class="navbar-brand" href="/">
          <img src="{{asset('images/logo-white.png')}}" class="navbar-brand-png" width="75px;" height: "100px;" alt="logo
            chapelle">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse text-center" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active fw-bold fs-2 text-blancCasse text-lg-start" aria-current="page" href="/">Chapelle-Curreaux</a>
            </li>
          </ul>
          <div class="navbar-nav d-flex justify-content-end" role="search">
          {% if app.user %}
            <button  class="btn btn-sable" type="submit"><a class="nav-link text-marronFonce fw-normal" href="{{path('user', {id: app.user.id}) }}">Mon compte</a></button>
            <button  class="btn btn-marronClaire" type="submit"><a class="nav-link text-sable fw-normal" href="{{ path('app_logout') }}">Déconexion</a></button>
          {% else %}
            <button  class="btn btn-sable" type="submit"><a class="nav-link text-marronFonce fw-normal" href="{{ path('app_login') }}">Connexion</a></button>
            <button  class="btn btn-marronClaire" type="submit"><a class="nav-link text-sable fw-normal" href="{{ path('app_register') }}">S'enregistrer</a></button>
          {% endif %}
          </div>
        </div>
      </nav>
      <h1 class="d-flex justify-content-center text-center text-marronFonce fw-bolder display-3 mt-5">La Réserve de</br>la chapelle</h1>
        <label for="exampleDataList" class="form-label text-center text-marronFonce fw-bold d-flex justify-content-center fs-2 my-5">Je Recherche...</label>
    <div class="d-flex justify-content-center">
  <input class="form-control w-50 " list="datalistOptions" id="exampleDataList" placeholder="Recherche...">
</div>
<datalist id="datalistOptions">
  <option value="San Francisco">
  <option value="New York">
  <option value="Seattle">
  <option value="Los Angeles">
  <option value="Chicago">
</datalist>

        <!-- <form class="w-50" role="search">
          <input class="form-control" type="search" placeholder="Search" aria-label="Search" />
          <button class="btn btn-outline-success" type="submit">
            Search
          </button>
        </form> -->
{# </div> #}
    {# </div> #}