<section class="mt-5">
    <h1 class="text-center text-marronClaire fw-bold">
    LIVRE : {{ book.title }}
    </h1>
{% for message in app.flashes('warning') %}
<div class="alert alert-warning mt-4 text-center">
 {{ message }}
</div>
{% endfor %}
<div class="container mt-5">
  <div class="row">
    <div class="col-6" style="background-image: url('{{ vich_uploader_asset(book, 'imageFile')}}'); background-position: center; background-repeat: no-repeat; background-size: cover; width=auto; height: 510px;">
    </div>
    <div class="col-6 text-center">
    <h2 class = "mt-3">
    {{ book.title }}
    </h2>
    <p class = "mt-5">{{ book.description }}</p>
    {% if app.user and app.user.is_confirmed and book.nb_of_book > 0 and show_bt %}
    {{ form(formReserved) }}
    {% else %}
     <button type="button" class="btn btn-marronClaire text-sable btn-lg mt-5" disabled>Réservation</button>
    {% endif %}
    </div>
  </div>
</div>
</section>